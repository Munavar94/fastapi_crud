# FastAPU CRUD
Full CRUD, MySQL Database, Auth &amp; Docker

# All packages at a time
pip install -r requirements.txt
or
pip freeze

# used for schema validation
pip install pydantic

# Swagger UI
http://127.0.0.1:8000/docs

# To Run:
uvicorn filename:initialize_variable --reload --port 8000
uvicorn main:app --reload
